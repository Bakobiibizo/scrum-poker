version: '3.8'

services:
  relay:
    build: .
    ports:
      - "8070:8070"
    environment:
      - NODE_ENV=production
      - PORT=8070
      - RELAY_URL=https://scrum-poker-hydra.ngrok.dev
    restart: unless-stopped
