version: '3.8'

services:
  relay:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - RELAY_URL=https://scrum-poker.hydra.ngrok.dev
    restart: unless-stopped
