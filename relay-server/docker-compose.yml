version: '3.8'

services:
  relay:
    build: .
    ports:
      - "8060:8060"
    environment:
      - NODE_ENV=production
      - PORT=8060
      - RELAY_URL=https://scrum-poker-hydra.ngrok.dev
    restart: unless-stopped
